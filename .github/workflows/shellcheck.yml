name: Repository checks

on: [push, pull_request]

permissions:
  contents: read

jobs:
  repository-quality:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install ShellCheck
        run: |
          sudo apt-get update
          sudo apt-get install --yes shellcheck

      - name: Lint shell scripts
        run: |
          find scripts -type f -name '*.sh' -print0 | xargs -0 --no-run-if-empty shellcheck

      - name: Test public-safe system output
        run: bash scripts/test-system-info-safety.sh

      - name: Test promotion manifest validator (synthetic fixtures only)
        run: python3 scripts/test_promotion_manifest_validator.py -v

      - name: Test local second-copy controls (synthetic mocks only)
        run: python3 scripts/test_local_second_copy.py -v

      - name: Check repository-relative Markdown links
        run: python3 scripts/check-markdown-links.py
